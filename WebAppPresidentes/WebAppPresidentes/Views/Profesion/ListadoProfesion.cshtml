@model IEnumerable<WebAppPresidentes.Models.Entidades.Profesion>

<br>
<h3>LISTADO DE PROFESIONES</h3>
<br>
<form>
    <div class="row">
        <div class="col-3">
            <input class="form-control input-sm" name="filterByNameio" placeholder="Buscar por Nombre">
        </div>
        <div class="col-4">
            <button class="btn btn-primary btn-sm">filtrar</button>
        </div>

        <div class="col-5">
        </div>
    </div>
</form>

<hr>
<a class="btn btn-success btn-sm" role="button" asp-action="Create">Agregar</a>
<br>
<hr>

<table class="table table-hover table-bordered table-sm">
    <thead>
        <tr class="table-info">
            <th><center><strong>Id</strong></center></th>
            <th><center><strong>Profesion</strong></center></th>
            <th><center><strong>Categoria</strong></center></th>
            <th><center><strong>Grado</strong></center></th>
            <th><center><strong>Especialidad</strong></center></th>
            <th><center><strong>Opciones</strong></center></th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td align="center">@item.IdProfesion</td>
                <td align="left">@item.NombreProfesion</td>
                <td align="left">@item.Categoria</td>
                <td align="left">@item.Grado</td>
                <td align="left">@item.Especialidad</td>

                <td>
                    <center>
                        <a class="btn btn-info btn-sm text-white" role="button" asp-action="Edit"
                       asp-route-id="@item.IdProfesion">Edit</a>

                        <a class="btn btn-warning btn-sm text-white" role="button"
                       asp-action="Details" asp-route-id="@item.IdProfesion">Detalles</a>

                        <a class="btn btn-danger btn-sm" role="button"
                       asp-action="Delete" asp-route-id="@item.IdProfesion">Delete</a>
                    </center>
                </td>
            </tr>
        }
    </tbody>
</table>
